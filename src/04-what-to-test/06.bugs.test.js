const examCalculator = () => ({
  sum: (a, b) => {
    return a + b
  },
})

// 🐛 Bug report:
// When adding '12' and '13' it returns '1213'
// Expected result: should return 25 as a string

describe(`Bugs`, () => {
  it(`should ...`, () => {
    // 💪 Just do it
  })
})
